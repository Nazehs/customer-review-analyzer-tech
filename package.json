{
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "~4.8.4",
    "@types/node": "^18.11.9"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
